// PMDesignSystem — Reusable SwiftUI components, colour tokens, task cards, badges.
// Depends on PMDomain for entity types (display purposes).
//
// Populated in Phase 4.

import SwiftUI
import PMDomain
import PMUtilities

public enum PMDesignSystemMarker {
    public static let version = "0.1.0"
}
